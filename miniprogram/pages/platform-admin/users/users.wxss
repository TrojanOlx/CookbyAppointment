@import "../platform-admin.wxss";

.footer-pad { height: 40rpx; }

.initials-avatar {
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--primary-dark);
  font-size: 28rpx;
  font-weight: 900;
}
